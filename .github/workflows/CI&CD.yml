name: Test, Build & Push (Node.js + Angular)

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  test-build-push:
    runs-on: ubuntu-latest

    steps:
      # -----------------------------
      # Step 1: Checkout repository
      # -----------------------------
      - name: Checkout code
        uses: actions/checkout@v4

      # -----------------------------
      # Step 2: Setup Node.js
      # -----------------------------
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # -----------------------------
      # Step 3: Backend - Install deps & run tests
      # -----------------------------
      - name: Backend - Install dependencies & run tests
        working-directory: ./petmanagement_backend_rajalakshmi
        run: |
          npm ci
          npm test

      # -----------------------------
      # Step 4: Frontend - Install deps & run Angular tests
      # -----------------------------
      - name: Frontend - Install dependencies
        working-directory: ./petmanagement_frontend_rajalakshmi
        run: |
          npm ci
          npm install -g @angular/cli

      - name: Frontend - Run unit tests
        working-directory: ./petmanagement_frontend_rajalakshmi
        run: |
          npx ng test --watch=false --browsers=ChromeHeadless

      # -----------------------------
      # Step 5: Login to Docker Hub
      # -----------------------------
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # -----------------------------
      # Step 6: Build & Push Backend Image
      # -----------------------------
      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./petmanagement_backend_rajalakshmi
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/findpet-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/findpet-backend:${{ github.sha }}

      # -----------------------------
      # Step 7: Build & Push Frontend Image
      # -----------------------------
      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./petmanagement_frontend_rajalakshmi
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/findpet-frontend:latest
            ${{ secrets.DOCKER_USERNAME }}/findpet-frontend:${{ github.sha }}
